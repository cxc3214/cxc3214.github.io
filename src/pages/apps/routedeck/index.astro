---
import AppLayout from "../../../layouts/AppLayout.astro";
import apps from "../../../config/apps.json";

const app = apps.find((item) => item.id === "routedeck")!;
---

<AppLayout app={app}>
  <h2>桌面就是控制入口</h2>
  <p>
    RouteDeck 可以注册为 Android HOME 桌面，管理多页布局、文件夹、应用抽屉和快捷入口，并把逐应用网络策略与模型服务放在同一个工作台中。
  </p>

  <h2>主要能力</h2>
  <ul>
    <li>适配手机和平板的多页桌面、文件夹、搜索、拖放与布局备份。</li>
    <li>按应用设置直连或已配置的线路策略，并显示当前状态。</li>
    <li>端上 Qwen 模型与多家在线模型服务，可由用户自行配置。</li>
    <li>模型凭据使用 Android Keystore 加密保存，关键整理操作先预览再确认。</li>
  </ul>

  <h2>权限与网络提示</h2>
  <p>
    网络权限用于模型服务、订阅和版本检查；通知权限用于运行状态；麦克风和相机仅在语音或图片输入时使用；VPN 权限仅在你主动启用逐应用网络策略时由 Android 系统确认。
  </p>
  <p class="compliance-callout">
    RouteDeck 只应连接你拥有合法授权的网络与服务。不得用于绕过网络管理、访问违法内容、侵害他人系统或从事未经授权的网络活动。
  </p>

  <h2>当前测试边界</h2>
  <p>
    0.2.3-preview 是 arm64-v8a 预览包，只适用于 Android/AOSP 设备。部分模型和网络能力依赖用户自备的合法服务与凭据；界面中的测试或演示状态不代表线路、供应商或服务质量承诺。
  </p>
</AppLayout>
